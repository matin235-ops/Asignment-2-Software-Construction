<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>API Documentation - Book Management System</title>
    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <!-- Bootstrap Icons -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.0/font/bootstrap-icons.css" rel="stylesheet">
    <!-- Prism.js for syntax highlighting -->
    <link href="https://cdnjs.cloudflare.com/ajax/libs/prism/1.29.0/themes/prism-tomorrow.min.css" rel="stylesheet">
    <style>
        .endpoint-card {
            border-left: 4px solid #0d6efd;
            transition: all 0.3s ease;
        }
        
        .endpoint-card:hover {
            transform: translateY(-2px);
            box-shadow: 0 0.5rem 1rem rgba(0, 0, 0, 0.15);
        }
        
        .method-badge {
            min-width: 80px;
            font-family: 'Courier New', monospace;
            font-weight: bold;
        }
        
        .code-block {
            background: #2d3748;
            border-radius: 0.375rem;
            padding: 1rem;
            margin: 0.5rem 0;
        }
        
        .hero-section {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            color: white;
            padding: 4rem 0;
        }
        
        .copy-btn {
            position: absolute;
            top: 0.5rem;
            right: 0.5rem;
            opacity: 0.7;
            transition: opacity 0.3s ease;
        }
        
        .copy-btn:hover {
            opacity: 1;
        }
        
        .code-container {
            position: relative;
        }
    </style>
</head>

<body>
    <!-- Navigation -->
    <nav class="navbar navbar-expand-lg navbar-dark bg-primary">
        <div class="container">
            <a class="navbar-brand" href="/">
                <i class="bi bi-book"></i> Book Management System
            </a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav ms-auto">
                    <li class="nav-item">
                        <a class="nav-link" href="/">
                            <i class="bi bi-house"></i> Home
                        </a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/books/add">
                            <i class="bi bi-plus-circle"></i> Add Book
                        </a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link active" href="/api">
                            <i class="bi bi-code-slash"></i> API
                        </a>
                    </li>
                </ul>
            </div>
        </div>
    </nav>

    <!-- Hero Section -->
    <section class="hero-section">
        <div class="container text-center">
            <h1 class="display-4 fw-bold">
                <i class="bi bi-code-slash"></i> API Documentation
            </h1>
            <p class="lead">RESTful API for Book Management System</p>
            <div class="row justify-content-center mt-4">
                <div class="col-md-8">
                    <div class="card bg-white bg-opacity-10 border-0">
                        <div class="card-body">
                            <h5 class="text-white">Base URL</h5>
                            <code class="text-warning fs-5">http://localhost:3000/api</code>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </section>

    <!-- Main Content -->
    <div class="container mt-5">
        <!-- Quick Start -->
        <section class="mb-5">
            <h2 class="mb-4">
                <i class="bi bi-rocket"></i> Quick Start
            </h2>
            <div class="row">
                <div class="col-md-6">
                    <div class="card">
                        <div class="card-body">
                            <h5 class="card-title">
                                <i class="bi bi-terminal"></i> Test with curl
                            </h5>
                            <div class="code-container">
                                <button class="btn btn-sm btn-outline-secondary copy-btn" onclick="copyToClipboard(this)">
                                    <i class="bi bi-clipboard"></i>
                                </button>
                                <pre class="code-block"><code>curl http://localhost:3000/api/books</code></pre>
                            </div>
                        </div>
                    </div>
                </div>
                <div class="col-md-6">
                    <div class="card">
                        <div class="card-body">
                            <h5 class="card-title">
                                <i class="bi bi-browser-chrome"></i> Test in Browser
                            </h5>
                            <p class="card-text">
                                Visit:
                                <a href="/api/books" target="_blank">
                                    <code>/api/books</code>
                                </a>
                            </p>
                        </div>
                    </div>
                </div>
            </div>
        </section>

        <!-- API Endpoints -->
        <section class="mb-5">
            <h2 class="mb-4">
                <i class="bi bi-list-ul"></i> API Endpoints
            </h2>

            <!-- GET /api/books -->
            <div class="card endpoint-card mb-4">
                <div class="card-header">
                    <div class="d-flex align-items-center">
                        <span class="badge bg-success method-badge me-3">GET</span>
                        <code class="fs-5">/api/books</code>
                        <span class="ms-auto text-muted">Get all books</span>
                    </div>
                </div>
                <div class="card-body">
                    <p>Retrieve all books with optional search and sorting.</p>
                    <h6>Query Parameters:</h6>
                    <ul>
                        <li><code>search</code> - Search by title or author</li>
                        <li><code>sort</code> - Sort by: title, author, year, genre</li>
                        <li><code>order</code> - Order: asc, desc</li>
                    </ul>
                    <h6>Examples:</h6>
                    <div class="code-container">
                        <button class="btn btn-sm btn-outline-secondary copy-btn" onclick="copyToClipboard(this)">
                            <i class="bi bi-clipboard"></i>
                        </button>
                        <pre class="code-block"><code>curl "http://localhost:3000/api/books"
curl "http://localhost:3000/api/books?search=tolkien"
curl "http://localhost:3000/api/books?sort=year&order=desc"</code></pre>
                    </div>
                </div>
            </div>

            <!-- POST /api/books -->
            <div class="card endpoint-card mb-4">
                <div class="card-header">
                    <div class="d-flex align-items-center">
                        <span class="badge bg-primary method-badge me-3">POST</span>
                        <code class="fs-5">/api/books</code>
                        <span class="ms-auto text-muted">Add a new book</span>
                    </div>
                </div>
                <div class="card-body">
                    <p>Create a new book in the collection.</p>
                    <h6>Request Body:</h6>
                    <div class="code-container">
                        <button class="btn btn-sm btn-outline-secondary copy-btn" onclick="copyToClipboard(this)">
                            <i class="bi bi-clipboard"></i>
                        </button>
                        <pre class="code-block"><code class="language-json">{
  "title": "The Hobbit",
  "author": "J.R.R. Tolkien",
  "genre": "Fantasy",
                        "publicationYear": 1937,
  "favorite": true
}</code></pre>
                    </div>
                    <h6>Example:</h6>
                    <div class="code-container">
                        <button class="btn btn-sm btn-outline-secondary copy-btn" onclick="copyToClipboard(this)">
                            <i class="bi bi-clipboard"></i>
                        </button>
                        <pre class="code-block"><code>curl -X POST http://localhost:3000/api/books \
  -H "Content-Type: application/json" \
  -d '{"title":"Dune","author":"Frank Herbert","genre":"Science Fiction","publicationYear":1965}'</code></pre>
                    </div>
                </div>
            </div>

            <!-- GET /api/books/:id -->
            <div class="card endpoint-card mb-4">
                <div class="card-header">
                    <div class="d-flex align-items-center">
                        <span class="badge bg-success method-badge me-3">GET</span>
                        <code class="fs-5">/api/books/:id</code>
                        <span class="ms-auto text-muted">Get specific book</span>
                    </div>
                </div>
                <div class="card-body">
                    <p>Retrieve a specific book by its ID.</p>
                    <div class="code-container">
                        <button class="btn btn-sm btn-outline-secondary copy-btn" onclick="copyToClipboard(this)">
                            <i class="bi bi-clipboard"></i>
                        </button>
                        <pre class="code-block"><code>curl http://localhost:3000/api/books/{book-id}</code></pre>
                    </div>
                </div>
            </div>

            <!-- PUT /api/books/:id -->
            <div class="card endpoint-card mb-4">
                <div class="card-header">
                    <div class="d-flex align-items-center">
                        <span class="badge bg-warning method-badge me-3">PUT</span>
                        <code class="fs-5">/api/books/:id</code>
                        <span class="ms-auto text-muted">Update book</span>
                    </div>
                </div>
                <div class="card-body">
                    <p>Update an existing book's information.</p>
                    <div class="code-container">
                        <button class="btn btn-sm btn-outline-secondary copy-btn" onclick="copyToClipboard(this)">
                            <i class="bi bi-clipboard"></i>
                        </button>
                        <pre class="code-block"><code>curl -X PUT http://localhost:3000/api/books/{book-id} \
  -H "Content-Type: application/json" \
  -d '{"title":"Updated Title","favorite":true}'</code></pre>
                    </div>
                </div>
            </div>

            <!-- DELETE /api/books/:id -->
            <div class="card endpoint-card mb-4">
                <div class="card-header">
                    <div class="d-flex align-items-center">
                        <span class="badge bg-danger method-badge me-3">DELETE</span>
                        <code class="fs-5">/api/books/:id</code>
                        <span class="ms-auto text-muted">Delete book</span>
                    </div>
                </div>
                <div class="card-body">
                    <p>Remove a book from the collection.</p>
                    <div class="code-container">
                        <button class="btn btn-sm btn-outline-secondary copy-btn" onclick="copyToClipboard(this)">
                            <i class="bi bi-clipboard"></i>
                        </button>
                        <pre class="code-block"><code>curl -X DELETE http://localhost:3000/api/books/{book-id}</code></pre>
                    </div>
                </div>
            </div>

            <!-- PATCH /api/books/:id/favorite -->
            <div class="card endpoint-card mb-4">
                <div class="card-header">
                    <div class="d-flex align-items-center">
                        <span class="badge bg-info method-badge me-3">PATCH</span>
                        <code class="fs-5">/api/books/:id/favorite</code>
                        <span class="ms-auto text-muted">Toggle favorite</span>
                    </div>
                </div>
                <div class="card-body">
                    <p>Toggle the favorite status of a book.</p>
                    <div class="code-container">
                        <button class="btn btn-sm btn-outline-secondary copy-btn" onclick="copyToClipboard(this)">
                            <i class="bi bi-clipboard"></i>
                        </button>
                        <pre class="code-block"><code>curl -X PATCH http://localhost:3000/api/books/{book-id}/favorite</code></pre>
                    </div>
                </div>
            </div>

            <!-- GET /api/stats -->
            <div class="card endpoint-card mb-4">
                <div class="card-header">
                    <div class="d-flex align-items-center">
                        <span class="badge bg-success method-badge me-3">GET</span>
                        <code class="fs-5">/api/stats</code>
                        <span class="ms-auto text-muted">Get statistics</span>
                    </div>
                </div>
                <div class="card-body">
                    <p>Get collection statistics and overview.</p>
                    <div class="code-container">
                        <button class="btn btn-sm btn-outline-secondary copy-btn" onclick="copyToClipboard(this)">
                            <i class="bi bi-clipboard"></i>
                        </button>
                        <pre class="code-block"><code>curl http://localhost:3000/api/stats</code></pre>
                    </div>
                </div>
            </div>
        </section>

        <!-- Response Format -->
        <section class="mb-5">
            <h2 class="mb-4">
                <i class="bi bi-code-square"></i> Response Format
            </h2>
            <div class="card">
                <div class="card-body">
                    <p>All API responses follow this standard format:</p>
                    <div class="code-container">
                        <button class="btn btn-sm btn-outline-secondary copy-btn" onclick="copyToClipboard(this)">
                            <i class="bi bi-clipboard"></i>
                        </button>
                        <pre class="code-block"><code class="language-json">{
  "success": true,
  "message": "Operation completed successfully",
  "data": { ... },
  "count": 5
}</code></pre>
                    </div>
                </div>
            </div>
        </section>

        <!-- Sample Book Object -->
        <section class="mb-5">
            <h2 class="mb-4">
                <i class="bi bi-file-code"></i> Sample Book Object
            </h2>
            <div class="card">
                <div class="card-body">
                    <div class="code-container">
                        <button class="btn btn-sm btn-outline-secondary copy-btn" onclick="copyToClipboard(this)">
                            <i class="bi bi-clipboard"></i>
                        </button>
                        <pre class="code-block"><code class="language-json">{
  "id": "123e4567-e89b-12d3-a456-426614174000",
  "title": "Sample Book Title",
  "author": "Author Name",
  "genre": "Fiction",
  "publicationYear": 2023,
  "favorite": false,
  "createdAt": "2025-06-13T10:00:00.000Z"
}</code></pre>
                    </div>
                </div>
            </div>
        </section>

        <!-- Test Links -->
        <section class="mb-5">
            <h2 class="mb-4">
                <i class="bi bi-play-circle"></i> Live API Testing
            </h2>
            <div class="row">
                <div class="col-md-6 mb-3">
                    <a href="/api/books" target="_blank" class="btn btn-outline-primary w-100">
                        <i class="bi bi-arrow-right-circle"></i> Test GET /api/books
                    </a>
                </div>
                <div class="col-md-6 mb-3">
                    <a href="/api/stats" target="_blank" class="btn btn-outline-info w-100">
                        <i class="bi bi-bar-chart"></i> Test GET /api/stats
                    </a>
                </div>
            </div>
        </section>
    </div>

    <!-- Footer -->
    <footer class="bg-light mt-5 py-3">
        <div class="container text-center">
            <p class="mb-0 text-muted">
                <i class="bi bi-book"></i> Book Management System API &copy; 2025
            </p>
        </div>
    </footer>

    <!-- Bootstrap JS -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    <!-- Prism.js for syntax highlighting -->
    <script src="https://cdnjs.cloudflare.com/ajax/libs/prism/1.29.0/components/prism-core.min.js"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/prism/1.29.0/plugins/autoloader/prism-autoloader.min.js"></script>

    <script>
        // Copy to clipboard functionality
        function copyToClipboard(button) {
            const codeBlock = button.parentNode.querySelector('code');
            const text = codeBlock.textContent;

            navigator.clipboard.writeText(text).then(function() {
                const originalIcon = button.innerHTML;
                button.innerHTML = '<i class="bi bi-check"></i>';
                button.classList.add('text-success');

                setTimeout(function() {
                    button.innerHTML = originalIcon;
                    button.classList.remove('text-success');
                }, 2000);
            }).catch(function() {
                alert('Failed to copy to clipboard');
            });
        }

        // Add fade-in animation
        document.addEventListener('DOMContentLoaded', function() {
            const cards = document.querySelectorAll('.endpoint-card');
            cards.forEach((card, index) => {
                card.style.opacity = '0';
                card.style.transform = 'translateY(20px)';

                setTimeout(() => {
                    card.style.transition = 'all 0.5s ease';
                    card.style.opacity = '1';
                    card.style.transform = 'translateY(0)';
                }, index * 100);
            });
        });
    </script>
</body>

</html>